# Ejecutar plan (Codex)

Lee el plan `.harness/plans/<ruta exacta>` completo y ejecuta su handoff en orden. Si falta la ruta, lista `.harness/plans/` priorizando `pendiente`/`en progreso` y pide aclaración si hay más de uno plausible. Edita del plan solo `estado` y la sección final `## Historial de commits` al concluir verificado.

Plan:
